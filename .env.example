PORT=4000
# Required: absolute/local path to the single shared workbook file.
ONE_DRIVE_XLSX_PATH=
ACK_WRITES_ENABLED=false
OPENAI_API_KEY=
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe
OPENAI_QUESTION_MODEL=gpt-5-nano
OPENAI_TRANSCRIBE_LANGUAGE=
# Keep false in production. Used for deterministic text-based AI test/demo endpoint.
ENABLE_AI_TEXT_TEST_ENDPOINT=false
